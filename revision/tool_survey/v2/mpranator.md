# MPRAnator — v2 capability record

**Slug:** `mpranator`
**Full name:** MPRAnator (modules: MPRA Motifs, MPRA SNPs, Transmutation, PWM Seq-Gen)
**Citation key:** `GeorgakopoulosSoares2017gb`
**Tier:** 2
**Row set:** v2 (`ROWS_v2.md`) — 33 keys. `hgvs_input` dropped; `library_as_object` split into
`library_first_class_object` + `library_algebra`; `mixed_mutagenesis_one_pool` split into four;
`dag_chaining` → `composable_operations`, `lazy_evaluation` → `lazy_generation` (renames);
D4–D7 and E3–E5 new.
**Basis:** `final/mpranator.md` (FINAL, adversarially reviewed, third independent verification pass
2026-08-10, ~30 live HTTP requests to genomegeek.com). No new web checks were needed — every v2 row
including all seven new rows is already resolvable from the FINAL memo's §3, §4, §6 and §7.
**Date:** 2026-08-10

---

## Block A — library specification

| key | value | evidence | source |
|---|---|---|---|
| `library_first_class_object` | **no** | ⚠️ *changed from `library_as_object` = partial.* There is no library object. No importable module, no CLI, no package (PyPI 404, bioconda 404, no `setup.py`/`requirements.txt`/`Dockerfile` in the 44-entry recursive tree; the only entry point is Django's `manage.py`). The sole user-facing representation of a designed library is a plain-text FASTA blob in a browser window or a downloaded `.fa` file — live docs: "The result page displays the synthesized oligonucleotides in the FASTA format. The user is able to download ( plain text file ) the generated oligonucleotides." `mycustom.py` defines `FastaFile`/`Sequence` classes but these are *input* parsers internal to the Django app and are never exposed. The v1 `partial` was awarded solely because one POST returns an entire multi-sequence library rather than one sequence at a time (verified live: a single SNP POST returned all variants + the WT entry; a single PWM POST returned every above-threshold k-mer) — that is a statement about batch generation, not about holding, inspecting, subsetting or passing on a library, and the FINAL memo itself records that "a strict 'is there a library object?' reading gives `no`". Under the v2 wording, which explicitly contrasts an object with "a tool that only writes files", MPRAnator is the latter. | live docs page; live POSTs to `/MPRA/SNPs/Results/` and `/MPRA/PWM/Results/`; `mycustom.py`; `iliasApp/views.py`; PyPI 404; bioconda 404; recursive repo tree |
| `composable_operations` | **partial** | *Rename of `dag_chaining`; value and evidence carried unchanged.* Two **author-documented** hand-offs, both copy-paste. Live docs, PWM section (verbatim): "The output of PWM Seq-Gen can be inputted into MPRAs Motifs tool, therefore allowing for the design of MPRA experiments using PWMs." Supp. §2.3 (verbatim): the Transmutation options serve "to generate negative controls for the MPRA SNP and Motif design tools". The four modules are genuinely independent rather than a locked pipeline, so a user *can* chain them by hand. **But no composition primitive exists** — no pipeline object, no graph, no nesting: `iliasApp/urls.py` exposes four independent views and the live site has four independent `<form action=…>` targets, each one POST returning a terminal FASTA blob. The only intra-module "composition" is the jQuery-UI drag-and-drop `ordering` of sub-components (barcode / background / 2 restriction sites / 2 adapters) — linear concatenation of fixed slots, not arbitrary composition. Convention C3. | live docs page; Supp. §2.3 (docx); `iliasApp/urls.py`; live HTML of all four form pages |
| `lazy_generation` | **no** | *Rename of `lazy_evaluation`; value and evidence carried unchanged.* Everything is materialised eagerly. `oligo.py::oligo()` builds `allResults = []` over the full `itertools.product(range(distanceFromLeftEdge, len(BackgroundS)), repeat=len(motifsL))` enumeration and appends every passing sequence; `part1.py::createMPRAResultOutput()` concatenates the whole library into a single `response` string; `views.py` returns it as one `text/plain` `HttpResponse`. **No generator, iterator or streaming anywhere in the tree.** This is precisely why the hard caps exist: `maximumNumberOfMotifsTimesSequences = 100`, `sequenceLimit` 1000 (Motifs) / 50000 (SNPs), `sequenceLengthLimit = 20000` (`forms.py` lines 264/307/481/492). | `oligo.py`; `part1.py`; `iliasApp/views.py`; `iliasApp/forms.py` |
| `library_algebra` | **no** | ⚠️ *new half of the `library_as_object` split.* No stack/concat/sample of whole libraries exists as an operation. Each module emits one terminal FASTA blob per POST; assembling a final pool from more than one module — e.g. Transmutation negative controls alongside a Motifs design, which is the tool's own documented workflow (Supp. §2.3) — requires the user to concatenate downloaded text files with an external script or editor. There is no library input to any module other than raw FASTA re-paste, no union/subset/sample operator, no repeat-library operator, and no library-level view at all. **Two near-misses recorded so a referee cannot say they were ignored, neither of which is a library operation:** (i) barcode multiplicity replicates every designed sequence N times with distinct barcodes in one pass (Supp. §2.1: "the user can select the multiplicity of each sequence to optimize the experiment"), and (ii) PWM Seq-Gen's `dupMotifs` toggle removes duplicates library-wide (docs: "Remove duplicates: Allows users to either keep only unique Sequence Motifs or include Duplicates as well"; Supp. §2.4). Both are *generation-time form parameters* consumed before any output exists, not operations applicable to a library the user holds — consistent with `library_first_class_object = no`. Scored `no` under the v2 criterion "`library_algebra` is 'no' if combining libraries requires an external script". | `iliasApp/urls.py`; `iliasApp/views.py`; live HTML of all four forms; Supp. §2.1, §2.3, §2.4 (docx); live docs (`dupMotifs`); `part1.py` |
| `exhaustive_single_scans` | **partial** | ⚠️ *new row from the `mixed_mutagenesis_one_pool` split (v1 value `partial`).* MPRAnator performs genuine exhaustive positional enumeration, but of **motif substitutions**, never of nucleotides. Motifs module with a single motif: `oligo.py::oligo()` enumerates every start position via `itertools.product(range(distanceFromLeftEdge, len(BackgroundS)), repeat=len(motifsL))`, filtered by `testIfWithin` (min/max spacing) and `testIfFarFromRightEdge`, with `frequencyOfInsertion` ("Interval of substitution … determines the frequency of motif substitutions in consecutive sequences", Supp. Fig. 1) thinning the scan — i.e. one substitution event walked across every admissible position of the scaffold. **What is absent is the row's literal capability:** there is no every-base-substitution mode, no every-position-deletion mode, no saturation/alanine/codon scan of any kind, and no "scan" concept in the paper, the Supplementary, the docs or the code. Variant input to the SNP module is a **user-supplied** VCF-like table (`mycustom.SnpFile`, ≥5 columns) — MPRAnator substitutes the variants it is given and never enumerates the possible variants at a position. PWM Seq-Gen's exhaustive mode enumerates all k-mers above a probability threshold, which is exhaustive enumeration of a *generative* model, not a mutagenesis scan. `partial` under convention C3 (a real, user-visible sub-step of the capability); a strict nucleotide-level reading gives `no`. | `oligo.py`; `part1.py`; Supp. Fig. 1 and §2.2, §2.4 (docx); `mycustom.py::SnpFile`; live Motifs and SNPs form HTML; full-tree grep (no `scan`/`saturat`/`codon`) |
| `sampled_random_mutagenesis` | **yes** | ⚠️ *new row from the split.* Random mutagenesis is a first-class, separately documented module. **Transmutation** applies N random point mutations to each input sequence: `part3.mutateString` chooses positions at random, skipping already-chosen positions and `N` bases (`while sequenceS[posChosen] == "N" or posChosen in positionsChosenL`), and refuses an all-N input ("Please do not enter a string only containing N"); it also offers scramble / reverse / complement. Supp. §2.3 describes the module and the header records the choices — reproduced live 2026-08-10: `>sequence1| Mutated_nucleotides - 3 | Scrambled - No | Reversed - No | Complemented - No`. **PWM Seq-Gen** independently provides probabilistic sampling: simulation mode draws k-mer realisations from a PWM (`simNum` replicates; live output `>MA0056.1 MZF1_1-4 | Simulation number - 1` / `GGGGGA`). Both modules verified fully functional live. **Scope caveats:** the control is a *count* of mutations per sequence (`numToMutate`), not a per-base rate or a spectrum/transition-transversion model; and because it is a separate module it cannot be requested inside the same specification as motif or SNP design (see `heterogeneous_components_one_library`). | `part3.py::mutateString`; Supp. §2.3, §2.4 (docx); live POSTs to `/MPRA/TransmutationResults/` and `/MPRA/PWM/Results/` (both modes) on 2026-08-10; live Transmutation form HTML |
| `higher_order_combinatorial` | **yes** | ⚠️ *new row from the split.* Pairwise and higher-order combinations in the same sequence are the tool's core behaviour, in **both** design modules. Motifs, Supp. §2.1 (verbatim): "If multiple motifs are inputted, sequences will be generated for each motif separately and for the motif combinations. For instance, if three motifs are inputted, output sequences for single, pairs and triplets of motifs will be generated." Implementation: `part1.py::generateCombinations` (all subsets via `itertools.combinations`) → `generatePermutations` (all orderings within each subset) → `oligo.py::oligo()` (all admissible position tuples), with `equaliseSize(..., 4)` capping the order at 4 motifs. SNPs, Supp. §2.2 (verbatim): "Although the default is to substitute all combinations of SNPs in the input sequences, there is also the option to substitute only a single SNP at a time." Transmutation additionally places N simultaneous point mutations in one sequence. **Caveats:** ≤4 motifs per design; the live SNP form exposes combinations as an opt-in checkbox ("Make Snp Combinations?") contradicting the Supplementary's stated default; and SNP-combination mode returns **HTTP 500** on the live service as of 2026-08-10 — value retained under convention C5 (designed + documented + implemented keeps the capability; the outage lives in availability). | Supp. §2.1, §2.2 (docx); `part1.py::generateCombinations`/`generatePermutations`/`equaliseSize`; `oligo.py`; `part3.py`; live SNPs form HTML; live POSTs 2026-08-10 |
| `heterogeneous_components_one_library` | **partial** | ⚠️ *new row from the split; inherits the v1 `partial` and its reasoning.* **Within a module, structurally different component types do coexist in one specification and one output.** Verified live in the SNP module: a single POST returned exhaustive single-SNP variants **and** a wild-type entry in the same blob — `>1 42990 43041| SNP rs43434 | Position 42991 | Nucleotide change T:G` alongside `>1 42990 43041| REFERENCE` (the WT entry is emitted without the user asking). With combinations enabled, singles + pairs + higher-order + WT coexist; barcode multiplicity adds N replicate copies of every member. The Motifs module likewise emits singles, pairs and triplets of motifs across many positions in one library. **Across modules it is impossible:** motif placement, SNP substitution and random/scrambled mutagenesis live in three separate forms with three separate submissions and three separate outputs, so a sampled/random or negative-control component can *never* be part of the same specification as a motif or SNP design — exactly the composite the v2 row description names ("exhaustive singles + sampled higher-order + WT replicates"). `partial`, not `yes`, for that reason. | Supp. §2.1, §2.2, §2.3 (docx); live POST to `/MPRA/SNPs/Results/` on 2026-08-10; `iliasApp/urls.py`; live HTML of all four forms |
| `combinatorial_motif_place` | **yes** | Unchanged from v1 — the strongest cell in the record. Supp. Fig. 1 caption (verbatim): "Up to 4 motifs are substituted into each of the background sequences using **all possible permutations**. Distance from the edges and minimum and maximum spacing between motifs restrict the positioning of motifs in the sequences. Interval of substitution (not shown) determines the frequency of motif substitutions in consecutive sequences." Code: `part1.py::generateCombinations` (all subsets), `generatePermutations` (all orderings), `equaliseSize(..., 4)`, `oligo.py::oligo()` enumerating position tuples with `itertools.product` filtered by `testIfWithin` and `testIfFarFromRightEdge`. Motif matching is IUPAC-aware (`myfunctions.regex_mapper`). **Orientation caveat:** one *global* `reverseComplement` checkbox ("Reverse complement sequence before motif substitution?") applied to the scaffold — not per-motif orientation enumeration. *Availability pairing:* the Motifs module returns HTTP 500 on every valid live submission as of 2026-08-10 (value retained under C5). | Supp. Fig. 1 (docx); `part1.py`; `oligo.py`; `myfunctions.py`; live Motifs form HTML; live POSTs 2026-08-10 |
| `barcode_generation` | **yes** | Unchanged from v1. Live form fields on **both** `/MPRA/Motifs/` and `/MPRA/SNPs/`: `barCodeLength`, `minimumGCContent`, `maximumGCContent`, `barCodeDistance`, `numOfBarCodesPerSequence` (live HTML re-verified: `numOfBarCodesPerSequence min="0" max="100"` on both; `barCodeLength min="10" max="25"` on Motifs). Docs tooltips (verbatim): "The Levenshtein distance between each barcode. The default is 2."; "This specifies the number of barcodes inserted per sequence (replicates)." Supp. §2.2: "select the barcode size (set to zero to exclude barcodes), minimum Levenshtein distance between barcodes and restrict the range of the barcode GC content." Implementation `part1.py::barcode_generator(number, mingc, maxgc, barCodeLength, diffs=2)` — "generates barcodes that differ from existing barcodes by a minimum number of bases, each sequence has a unique barcode"; barcodes are then attached as a drag-and-drop-orderable sub-component of the assembled oligo, and the barcode index is recorded in the header (`BARCODE` DESCRIPTOR). **Do not cite "≤5 barcodes/sequence" as the tool's limit** — that is the 2015 repo value (`forms.py: min_value=1, max_value=5`) and MPRAnator's own worked use case asks for six ("we use 6 different barcodes for each sequence", live docs). **Two precision caveats:** (i) the implementation counts position-wise identities (`if short_term[pos] == bc[pos]: count += 1`), a **Hamming**-style criterion, though docs and Supplementary say Levenshtein; (ii) **barcode generation is non-functional on the live service as of 2026-08-10** — every barcode-enabled POST to either module returned HTTP 500, including the site's own "Load Sample Data" values and both shipped API scripts. Value stays `yes` under convention C5; the outage belongs to `installable_today` / availability and must be cited alongside this cell. | live form HTML (both modules); live docs tooltips + Motif Use Case; Supp. §2.2 (docx); `part1.py::barcode_generator`; `iliasApp/forms.py`; ~10 live POSTs on 2026-08-10 |
| `per_sequence_provenance` | **partial** | Unchanged from v1. Every output sequence carries a structured, machine-parseable header. Live docs (verbatim): "A header is composed of one or more DESCRIPTORs and each DESCRIPTOR is composed of a LABEL and INFO. The descriptors are delimited by a |, i.e. a 'pipe'." Documented LABEL vocabulary: `<MOTIF>` (motif + substitution position), `BARCODE` ("The variant of the same sequence"), `RESTRICTION`, `ADAPTER`, `DUPLICATE_RESTRICTION_SITES`. All reproduced live: `>1 42990 43041| SNP rs43434 | Position 42991 | Nucleotide change T:G`; `>1 42990 43041| SNP rs_ins | … | removed 2 nucleotides from right edge`; `>1 42990 43041| SNP rs_del | Nucleotide change TTT:G| added 2 Adenines bases in the right edge`; `>sequence1| Mutated_nucleotides - 3 | Scrambled - No | Reversed - No | Complemented - No`; and a *quantitative* per-sequence score from PWM Seq-Gen: `> MA0056.1 MZF1_1-4 | Prob : 0.10965375000000001`. So provenance goes well beyond naming the mutation: which sub-components were added in what role, which construction-time warnings fired, the indel length-normalisation performed, and a probability. **But it is a delimited string inside a FASTA header — no TSV/JSON side-car, no per-sequence table, nothing queryable.** Hence `partial` (convention C3). This overturns the prior analysis's "Sequence metadata: None" — do **not** revert. | live docs page; live POSTs (SNPs incl. indels + restriction sites, Transmutation, PWM both modes) 2026-08-10; `parseSNPs.py::trimSequenceAndMakeHeader`; `oligo.py` |
| `automatic_naming` | **yes** | Unchanged from v1. Headers are generated entirely by the tool; the user never supplies them. `oligo.py`: `headerString = "|".join(["-".join([i[0], str(i[1])]) for i in header])`. Documented example (verbatim): "> ATGTG - 53|AAAAA-61|RESTRICTION - 1|RESTRICTION - 2", with "ATGTG - 53 is the motif starting at position 54 in the background sequence." Reproduced live for three of four modules: `>1 42990 43041| SNP rs43434 | Position 42991 | Nucleotide change T:G` (SNPs); `>sequence1| Mutated_nucleotides - 3 | …` (Transmutation); `>MA0056.1 MZF1_1-4 | Simulation number - 1` (PWM). | `oligo.py`; live docs; live POSTs 2026-08-10 |
| `design_visualization` | **partial** | Unchanged from v1. Highlighted sequences only; no design or graph view. Live docs (verbatim): "Image below is an excerpt of the Results page ( showing 8 nucleotides ). The nucleotides in red are the substituted motifs." Supp. §2.1: "Substituted motifs are colour-marked for visualization purposes."; §2.3: "Mutated nucleotides are colour-marked for visualization purposes." Implementation: `oligo.py` builds a parallel `sequenceHTML`; `myfunctions.py::highlightString` wraps positions in `<span style='color:%s;'>`. Verified live in raw HTML: a Transmutation run returned exactly 6 red spans for 3 mutated nucleotides × 2 sequences, e.g. `<span style='color:red;'>T</span>GAGAAGGACATATTC…`. **No graph/DAG view and no library-level summary plot** anywhere in the tree or on the live pages. Overturns the prior analysis's "Design cards: None" — do not revert. | live docs; Supp. §2.1, §2.3 (docx); `myfunctions.py::highlightString`; `oligo.py`; live Transmutation POST (raw HTML) 2026-08-10 |

## Block B — assay coverage

| key | value | evidence | source |
|---|---|---|---|
| `assay_dms` | **no** | Unchanged. Genuine absence, not an unfound feature. Zero-hit grep across the full recursive tree for `codon|amino|protein|translat|orf|reading.frame`; the same terms are absent from the PDF, the re-parsed Supplementary docx and the live documentation. Mutagenesis is strictly **nucleotide-level**: motif substitution, SNP/indel substitution, random point mutation, scramble/reverse/complement. Title and all four module names are MPRA-scoped. | full recursive repo tree (44 entries); PDF (PyMuPDF); Supp. docx; live docs |
| `assay_mpra` | **yes** | Unchanged. Title (verbatim): "MPRAnator: a web-based tool for the design of massively parallel reporter assay experiments." Abstract: "We introduce MPRAnator, a set of tools that facilitate rapid design of MPRA experiments." Supplementary records portability beyond MPRA: "the output sequences are not restricted to MPRA experiments, but they can easily be linked to similar protocols such as **BunDLE-seq** (Levo et al., 2015)." The Sanger landing page independently describes the same MPRA scope. | PDF title + abstract; Supp. §2.1 (docx); sanger.ac.uk/tool/mpranator/ |
| `assay_insilico` | **no** | Unchanged. No sequence-to-function model, no predictor and no model-in-the-loop design step anywhere in paper, Supplementary, docs or repo. PWM Seq-Gen generates sequences *from* a position weight matrix; a PWM is not a sequence-to-function model and nothing scores a designed library against one. *Deliberately not claimed:* that the tool "does not score" — it **does** emit a per-k-mer PWM probability in exhaustive mode (reproduced live, `> MA0056.1 MZF1_1-4 | Prob : 0.10965375000000001`; Supp. §2.4: the header "contains information regarding the probability of each k-mer occurring"). That is a motif-match probability, not an in-silico functional assay. | PDF; Supp. §2.4 (docx); live `/MPRA/PWM/Results/` in both modes 2026-08-10; full repo tree |

## Block C — genomics integration

| key | value | evidence | source |
|---|---|---|---|
| `genome_coordinates` | **partial** | Unchanged. The SNP module **requires** coordinates, but only as text in the FASTA header: `mycustom.py::FastaFileWithHeaderRange.getHeaderRange` parses `chr(\w+):(\d+)-(\d+)` and raises "Header does not contain chromosome and positions in the proper format. eg. 'chr1:start-end'" — reproduced live verbatim by submitting a plain header. `parseSNPs.py::snpInChromosomeRegion` then checks each VCF record against that interval. A `BedFile` parser exists in `mycustom.py`, and the homepage offers "a Python script that converts original coordinates to equal sized, centered coordinates with range X nucleotides on each side." **Key negative: MPRAnator never reads a reference genome.** The user must paste or upload the actual sequence; no genome build selector, no FASTA index, no coordinate→sequence retrieval. Chromosome validation is hard-coded human-only in `mycustom.SnpFile` (`[str(i) for i in range(1,23)] + ["X","Y"]`); `getHeaderRange` separately rejects only `int(chrNumber) > 23`. *Availability note:* `POST /MPRA/ScriptDownload/Bed/` → HTTP 500, so the coordinate-centering helper cannot currently be downloaded. | `mycustom.py`; `parseSNPs.py`; genomegeek.com homepage; live SNP POST with a malformed header; live `ScriptDownload` probes 2026-08-10 |
| `transcript_models` | **no** | Unchanged. Zero-hit grep across the full tree for `gtf|gff|exon|intron|splice|transcript`. `mycustom.py` contains exactly three parsers: `FastaFile`, `BedFile`, `SnpFile`. No gene/transcript/exon concept in the paper, Supplementary or live docs. Input formats are FASTA, a 5-column VCF-like table, BED intervals, and PWM count matrices. | full recursive repo tree; `mycustom.py`; PDF; Supp. docx; live docs |
| `exon_intron_split_codons` | **no** | Unchanged. Follows from two independently verified absences (no transcript model, no codon concept anywhere). Substitution is by **absolute offset into a linear sequence** — confirmed in `parseSNPs.py` and `oligo.py` positional slicing. | `parseSNPs.py`; `oligo.py`; the two rows above |
| `vcf_vep_output` | **no** | Unchanged. VCF is an **input** format only. Every live output obtained (SNPs, Transmutation, PWM in both modes) was plain-text FASTA, and every download path in `views.py` is `content_type='text/plain'`. Live docs: "The result page (plain text view) displays the synthesized oligonucleotides in FASTA format." No VCF, GFF or annotation-tool-consumable emission; VEP appears nowhere. The API scripts' `usingDownload` flag only switches to raw-text FASTA. | live POSTs (all four modules) 2026-08-10; `iliasApp/views.py`; live docs; `downloadables/*.py` |
| `consequence_annotation` | **no** | Unchanged. Headers record the nucleotide change and the indel length-normalisation performed — reproduced live, `| Nucleotide change TTT:G| added 2 Adenines bases in the right edge` — and nothing else. The documented LABEL vocabulary on the live docs page is exhaustive (`<MOTIF>`, `BARCODE`, `RESTRICTION`, `ADAPTER`, `DUPLICATE_RESTRICTION_SITES`) and contains no consequence term. With no gene model present, a molecular consequence could not be computed. | live SNP POSTs (SNV + insertion + deletion) 2026-08-10; live docs LABEL list; `transcript_models = no` |

*(`hgvs_input` was v1 `no` and is dropped from v2. Retained for the limitations prose: the only variant input is a whitespace-delimited VCF-like table; `mycustom.py::SnpFile` requires ≥5 columns — "less than 5 columns, probably not a VCF file" — and the string `HGVS` appears nowhere in the PDF, Supplementary, docs or source.)*

## Block D — adjacent / complementary

| key | value | evidence | source |
|---|---|---|---|
| `primer_design` | **no** | Unchanged. Zero-hit grep across the full tree for `primer|melting|anneal|PCR|\bTm\b`; the same terms are absent from the PDF, the Supplementary docx and the live docs. The only wet-lab-facing elements are **user-supplied fixed adapter and restriction-site strings**, concatenated verbatim by `part1.py::createMPRAResultOutput`. No oligo-assembly primer design, no Tm calculation, no overlap design. | full recursive repo tree; `part1.py`; PDF; Supp. docx; live docs |
| `codon_optimization` | **no** | Unchanged. No codon usage table, no organism selector, no synonymous-recoding logic anywhere — the same zero-hit `codon|amino|translat` grep that supports `assay_dms = no`. | full recursive repo tree; PDF; Supp. docx; live docs |
| `synthesis_constraints` | **partial** | Unchanged. Two real, documented, **narrow** behaviours. **(1) Restriction-site collision *reporting*:** live docs LABEL "DUPLICATE_RESTRICTION_SITES : The restriction site which has multiple copies present"; Supp. §2.1 "If any of the restriction sites are identified in any generated oligonucleotide sequence they are reported in the header of the corresponding sequence"; `part1.py`: `if len(myf.findMatch(...)) > 1: outputHeader += "|DUPLICATE_RESTRICTION_SITES - RESTRICTION1"`. It **reports; it does not repair or filter.** **(2) Uniform oligo length, explicitly motivated by synthesis** — Supp. §2.2 (verbatim): "Since several methods of oligonucleotide synthesis work best when all oligos have similar lengths, the instances with an insertion must be trimmed while the sequences with a deletion must be expanded. MPRAnator solves this problem by adding adenines to one end of the sequences that are too short, and by trimming one end of the sequences that are too long." Reproduced live in both directions. The Motifs module separately enforces `lengthsSame()` on inputs (live: "Sizes of the sequences should be the same"). **Absent:** no GC check on the full oligo (GC bounds apply to barcodes only), no homopolymer/repeat limit, no secondary-structure or synthesis-difficulty scoring, no vendor screening, no platform length cap. | live docs; Supp. §2.1, §2.2 (docx); `part1.py`; `parseSNPs.py::trimSequenceAndMakeHeader`; `myfunctions.py`; live SNP indel POSTs; live unequal-length Motifs POST |
| `degenerate_iupac_codons` | **yes** | ⚠️ *NEW row.* IUPAC/degenerate-base handling is pervasive and documented — `ROWS_v2.md` names MPRAnator as an exemplar for this row. Live docs, verbatim: "The input will accept all nucleic acid IUPAC letters." (a) **Expansion for matching:** `myfunctions.regex_mapper` expands R/Y/S/W/K/M/B/D/H/V/N into regex character classes (`'R': '[A|G]'`, `'N': '[A|T|G|C]'`, …), so motif substitution/search and restriction-site collision detection are degenerate-code aware. (b) **Degenerate-aware mutagenesis:** `myfunctions.mapperDict` defines the legal substitutions for every IUPAC code (`"M": 'GT'`, `"W": 'GC'`, …) so `part3.mutateString` mutates a degenerate position to a base consistent with that code, and it **explicitly skips `N` positions** (`while sequenceS[posChosen] == "N" or posChosen in positionsChosenL`), refusing an all-N input ("Please do not enter a string only containing N"). (c) **Degenerate complement:** `myfunctions.complement` carries the full IUPAC complement map. **Scope caveat (the reason to flag this cell):** all of this is **nucleotide-level**, not codon-level — MPRAnator has no codon concept at all (zero-hit `codon|amino|translat` grep; see `assay_dms`), so under a strictly codon-scoped reading of the row this cell would be `no` and the capability would move to a nucleotide-degeneracy footnote. Scored `yes` on the row as written ("degenerate/IUPAC codon design, expansion, or compression") and on the row set's own designation of MPRAnator as a positive example. *Not claimed:* compression of a variant set into a degenerate template — MPRAnator only consumes and expands degenerate codes, it never emits a compressed degenerate design. | `myfunctions.py::regex_mapper`/`mapperDict`/`complement`; `part3.py::mutateString`; live docs ("all nucleic acid IUPAC letters"); `ROWS_v2.md` row D4; full-tree `codon` grep (zero hits) |
| `negative_control_generation` | **yes** | ⚠️ *NEW row.* A first-class feature: one of MPRAnator's four modules exists for exactly this purpose. **Transmutation** offers scramble, reverse, complement and N random point mutations, and Supp. §2.3 states the purpose verbatim — these options serve "**therefore serving to generate negative controls for the MPRA SNP and Motif design tools**". The choices are recorded per sequence in the header, reproduced live 2026-08-10: `>sequence1| Mutated_nucleotides - 3 | Scrambled - No | Reversed - No | Complemented - No`, with mutated bases colour-marked (6 red `<span>`s for 3 mutations × 2 sequences). The module is **fully functional live**. Separately, the SNP module emits a wild-type/reference entry alongside every variant without being asked (`>1 42990 43041| REFERENCE`, verified live) — a positive-control/baseline arm generated automatically. **Caveat:** controls are produced by a *separate submission* and must be concatenated with the design output by hand (see `library_algebra`); the repo snapshot's `Part3Form` predates the live site and lacks the reverse/complement options the Supplementary and live form expose. | Supp. §2.3 (docx); live Transmutation form HTML + live POST to `/MPRA/TransmutationResults/` 2026-08-10; live SNP POST (`| REFERENCE`); `part3.py`; FINAL memo §4 item 4 |
| `ml_model_in_loop` | **no** | ⚠️ *NEW row.* No predictive model drives any design decision. There is no sequence-to-function predictor, no scoring of a designed library, no optimisation loop, no learned model of any kind, and no ML dependency — the 44-entry recursive tree has no `requirements.txt` at all, let alone a numerical/ML stack; the paper, Supplementary and live docs contain no prediction claim. **Near-miss recorded so it cannot be called an oversight:** PWM Seq-Gen's exhaustive mode enumerates k-mers whose PWM probability exceeds a user threshold and reports that probability per sequence (`> MA0056.1 MZF1_1-4 | Prob : 0.10965375000000001`; Supp. §2.4). That is a *generative* motif model used as its own sampler/filter — a motif-match probability, not a predictive model's functional output fed back into design — and the FINAL memo takes the deliberate position that "a PWM is not a sequence-to-function model and nothing scores a designed library against one". A referee arguing the threshold constitutes model-guided generation would get `partial`; the row as written (design driven by a *predictive* model, cf. ledidi/tangermeme) gives `no`. | full recursive repo tree (no ML dependency, no `requirements.txt`); PDF; Supp. §2.4 (docx); live `/MPRA/PWM/Results/` both modes 2026-08-10; FINAL memo §3 `assay_insilico` |
| `readout_analysis` | **no** | ⚠️ *NEW row.* MPRAnator is design-only. Its complete functional surface is four Django forms, each taking a design specification and returning a FASTA blob (FINAL memo §2); there is no read/FASTQ input anywhere, no demultiplexing, no barcode-to-variant mapping table emitted for downstream counting, no count/normalisation/statistics step, and no analysis claim in the paper, the Supplementary or the live docs. The barcode↔sequence association is only ever present as a `BARCODE` DESCRIPTOR inside a FASTA header (see `per_sequence_provenance`), never as a machine-readable key file for readout analysis. Every download path is `content_type='text/plain'` FASTA. | FINAL memo §2 (complete module enumeration) and §7; `iliasApp/urls.py`; `iliasApp/views.py`; full recursive repo tree; PDF; Supp. docx; live docs |

## Block E — engineering and availability
*(matrix value is `yes` by row-set convention; the substantive answer is the descriptive string)*

| key | value | descriptive answer / evidence | source |
|---|---|---|---|
| `interface` | **yes** | **"Web GUI + HTTP-POST form API (2 of 4 modules); no library API, no CLI, no installable package."** Surfaces: (a) a **Django web GUI** with jQuery-UI drag-and-drop sub-component ordering; (b) an HTTP-POST "REST API" — paper: "The REST API allows programmatic access to MPRAnator using simple URLs" — implemented in practice as two downloadable scripts that `requests.post()` the same form parameters and write the FASTA response to a file. No library API, no CLI, no package: PyPI 404, bioconda 404, and the 44-entry tree contains no `setup.py`, `requirements.txt` or `Dockerfile`; the only entry point is Django's `manage.py`. The repo's `MpraSnpApiView` is a stub returning the literal string `"testing"` and `restView` just redirects home. Only **2 of 4** modules have any programmatic access. The repo script copies are Python 2 with the dead `MPRAResults/` endpoint; the copies **served live today** use `print(...)` and current endpoints, but **both 500 as distributed**, so the paper's REST-API claim has no working demonstrator (2026-08-10). Robustness note: the live forms submit a JS-generated `ordering` field absent from the static HTML, and a POST omitting it returns HTTP 500 rather than a validation error. *(v1 value `partial` was a matrix-axis judgement; under the v2 convention that Block E carries `yes` + a descriptive string, the string above is what does the work.)* | live `/MPRA/ScriptDownload/{MpraMotifs,MpraSnps}/`; `downloadables/*.py`; `iliasApp/views.py`; `iliasApp/urls.py`; PDF Availability section; PyPI 404; bioconda 404; recursive tree; ~30 live POSTs 2026-08-10 |
| `license` | **yes** | **"MIT per the paper; no LICENSE file in the repository (GitHub API reports `license: null`)."** Paper, Availability section (verbatim): "The source code is available on www.github.com/hemberg-lab/MPRAnator/ under the MIT license." Repository reality: no `LICENSE` or `COPYING` file anywhere — confirmed against the root listing and the 44-entry recursive tree — and no license shown in the GitHub sidebar. The declaration is public and made by the copyright holders, so this is a documentation gap, not a closed-source tool (convention C4: declared-but-not-shipped). | PDF Availability section (PyMuPDF); GitHub repo root listing + sidebar; recursive git tree; GitHub API `license: null` |
| `installable_today` | **yes** | **"Not installable — no pip, no conda, no Docker, no CLI; hosted web service only, and only 2 of its 4 modules function (2026-08-10)."** PyPI `https://pypi.org/pypi/mpranator/json` → 404; bioconda `recipes/mpranator/` → 404; no `setup.py`, no `requirements.txt`, no `Dockerfile`, no install section in the 12-byte `README.md` ("# MpraNator"), no CLI. The code is Python 2 (`print "…"`, `xrange`) on Django 1.x (`django.conf.urls.patterns`) with `pdb.set_trace()` left in `parseSNPs.py` and a hard-coded interpreter path `#!/Users/Naman/miniconda/bin/python` in `oligo.py`, so a self-host attempt would not run on a current interpreter; and the public repo **predates the published tool** (no PWM Seq-Gen route or code), so the deployed source is not public. The only usable route is `genomegeek.com`, where — verified from three independent hosts/sessions across ~20 payloads on 2026-08-10 — **Transmutation and PWM Seq-Gen work fully, MPRA SNPs works only with barcodes and SNP-combinations both disabled, and MPRA Motifs (the module the paper leads with) returns HTTP 500 on every valid submission**, as does all barcode generation and the BED helper download. Phrasing discipline for the rebuttal: always date-stamp ("observed to return HTTP 500 on 2026-08-10"), never "the tool is broken". | PyPI 404; bioconda 404; recursive repo tree; `README.md`; `parseSNPs.py`; `oligo.py`; `iliasApp/urls.py`; ~30 live HTTP requests to genomegeek.com 2026-08-10; FINAL memo §7 |
| `last_activity` | **yes** | **"2015-12-27 — the only commit ever; repository ~10.6 years stale as of 2026-08-10; 0 releases, 0 tags."** Exactly one commit: `2015-12-27T14:57:53Z`, author Naman Jain, message "first commit"; `commits/master.atom` contains a single entry (updated 2015-12-27T15:28:58Z); the repo page shows 1 commit, 0 releases, 0 tags, 1 star, 1 fork, 0 open issues, 79 KB, not archived (`pushed_at` 2015-12-27T15:29:04Z, `updated_at` 2016-01-20). GitHub code search for `MPRAnator` returns `total_count: 1` — no fork or successor. **Counterweight, stated in fairness:** the *deployed service* is demonstrably newer than the repository (it serves a PWM Seq-Gen module absent from the public code and Python-3-syntax API scripts with current endpoints), so some post-2015 maintenance happened; it is simply not public and is not dated. v1 scored the old `maintained` row `no` under convention C1 (>5 y dormant). | `github.com/hemberg-lab/MPRAnator` repo page + `commits/master.atom`; GitHub code search; live `/MPRA/PWM/` and `/MPRA/ScriptDownload/*` 2026-08-10 |
| `documented_examples` | **yes** | **"6 — one 'Load Sample Data' preset per module (4), one fully worked use case on the documentation page, and one Supplementary sub-component-ordering figure; no vignettes, notebooks or test-suite examples."** Enumerated: (1) Motifs "Load Sample Data" (52 nt scaffold, two motifs, spacing/edge/interval values, 12 nt barcodes GC 20–80 %, 4 barcodes/sequence) — **returns HTTP 500 today**; (2) SNPs "Load Sample Data" (`>sequence1chr1:42990-43041`, two SNPs, 4 barcodes/sequence) — works **only** with barcodes disabled; (3) Transmutation "Load Sample Data" (two sequences, `numToMutate = 3`) — works; (4) PWM Seq-Gen "Load Sample Data" (two JASPAR matrices, `simNum = 3`) — works in both modes; (5) the **"MPRA Motif Use Case Example"** on the live docs page, the strongest example because it states an expected size — AP1/ELK1/RFX motifs into two 90 nt mm9 background tiles, 117 bp tiles, 15 bp barcodes GC 40–60 % edit distance 3, "we use 6 different barcodes for each sequence", "**This results in a total of 5856 sequences**"; (6) Supp. Fig. 3, modular sub-component ordering ("Sub-components can be placed in any order. Replicates of each sequence can be generated, each with distinct barcode sequence"). Also documented but not an example library: the BED coordinate-centering helper (download currently 500s). A `tests/` directory exists in the repo but ships no documented example library. | live forms (all four "Load Sample Data" presets); live documentation page (Motif Use Case); Supp. Fig. 3 (docx); `downloadables/*.py`; FINAL memo §8; live POSTs 2026-08-10 |

---

## How the v2 rows were resolved (delta from v1)

| v2 key | v1 key | v1 | v2 | Why |
|---|---|---|---|---|
| `library_first_class_object` | `library_as_object` | partial | **no** | The v1 `partial` credited "one POST returns a whole library" — batch generation, not an object. The sharper v2 wording ("an object the user can hold, inspect, transform, pass onward vs. a tool that only writes files") resolves to `no`: no object, no module, no package; a FASTA blob is the only representation. The FINAL memo already recorded that the strict reading gives `no`. |
| `library_algebra` | `library_as_object` | partial | **no** | New half of the split. No stack/concat/sample of libraries; combining a Transmutation control set with a Motifs design — the tool's own documented workflow — requires external concatenation, which the v2 criterion makes explicitly `no`. Near-misses (barcode multiplicity; PWM dedup) are generation-time parameters, not library operations. |
| `composable_operations` | `dag_chaining` | partial | **partial** | Rename; value and evidence carried unchanged. Two author-documented copy-paste hand-offs, no composition primitive. |
| `lazy_generation` | `lazy_evaluation` | no | **no** | Rename; carried unchanged. Fully eager materialisation into one in-memory string, which is why the size caps exist. |
| `exhaustive_single_scans` | `mixed_mutagenesis_one_pool` | partial | **partial** | Exhaustive positional enumeration exists, but the enumerated event is a *motif substitution*, not a nucleotide substitution/deletion. No saturation scan; SNPs are user-supplied. C3 `partial` with a scope note; strict nucleotide reading = `no`. |
| `sampled_random_mutagenesis` | `mixed_mutagenesis_one_pool` | partial | **yes** | Upgraded. Transmutation's N random point mutations + scramble, and PWM simulation mode, are both genuine sampling and both verified working live. Count-controlled rather than rate-controlled. |
| `higher_order_combinatorial` | `mixed_mutagenesis_one_pool` | partial | **yes** | Upgraded. All motif subsets ×  all permutations (singles/pairs/triplets, ≤4) and "all combinations of SNPs in the input sequences" are explicit higher-order combinations in the same sequence. |
| `heterogeneous_components_one_library` | `mixed_mutagenesis_one_pool` | partial | **partial** | Carries the v1 logic: singles + combinations + auto-emitted WT + barcode replicates coexist in one within-module output, but a sampled/random or control component can never share a specification with motif or SNP design. |
| `degenerate_iupac_codons` | — | new | **yes** | Pervasive IUPAC handling (regex expansion for matching, IUPAC substitution table for mutagenesis with explicit `N` skipping, IUPAC complement map, "input will accept all nucleic acid IUPAC letters"). Nucleotide-level, not codon-level. |
| `negative_control_generation` | — | new | **yes** | An entire module (Transmutation) whose stated purpose is "to generate negative controls for the MPRA SNP and Motif design tools", plus auto-emitted `| REFERENCE` WT entries. Verified working live. |
| `ml_model_in_loop` | — | new | **no** | No predictor, no scoring of designs, no ML dependency. PWM probability thresholding recorded as a near-miss. |
| `readout_analysis` | — | new | **no** | Design-only; no read input, demultiplexing, counting or statistics anywhere. |
| `installable_today` | — | new | **yes** (= "not installable") | PyPI/bioconda 404, no packaging files, Python 2/Django 1.x, deployed source not public; hosted service only, 2 of 4 modules functional. |
| `last_activity` | (`maintained` = no) | — | **yes** (= "2015-12-27") | Single commit ever; ~10.6 y. Deployed service is newer than the repo but closed-source and undated. |
| `documented_examples` | — | new | **yes** (= "6") | Four in-form presets + one worked use case (5856 sequences) + Supp. Fig. 3. |
| `hgvs_input` | `hgvs_input` | no | *dropped* | Per `ROWS_v2.md`; moved to limitations prose. |

**Unchanged from v1:** `combinatorial_motif_place` (yes), `barcode_generation` (yes), `per_sequence_provenance` (partial), `automatic_naming` (yes), `design_visualization` (partial), all of Block B, all of Block C, `primer_design` (no), `codon_optimization` (no), `synthesis_constraints` (partial), and the substance of `interface` / `license` (Block E now carries `yes` + the descriptive string by row-set convention, so the v1 `partial` labels are folded into those strings).

## Conventions relied on (must be applied identically to the other tools)

- **C1** — dormancy thresholds for `last_activity`/maintenance: >5 y dormant = negative.
- **C2** — code-present-but-UI-unreachable = `partial`. Not load-bearing here.
- **C3** — genuine, user-visible sub-step of a row = `partial` with a scope note, not `no`. Load-bearing for `composable_operations`, `exhaustive_single_scans`, `heterogeneous_components_one_library`, `per_sequence_provenance`, `design_visualization`. **Must be applied identically to the other web-only tools.**
- **C4** — declared-but-unshipped license = record both facts.
- **C5** — a capability that is designed, documented and implemented keeps its value even if the deployed service currently errors; the outage is recorded under availability. Load-bearing for `barcode_generation` (yes) and `combinatorial_motif_place` (yes), both of which return HTTP 500 live on 2026-08-10. **Whenever either cell is cited, the outage must be cited with it.**
- **New for v2** — `library_algebra` = `no` when combining libraries requires an external script.

## Least-confident cells (human review requested)

1. `library_first_class_object` = **no** — the one cell that moved down relative to v1. Justified by the sharper wording, but a referee-author will notice the change; the evidence string is written so the reason is unambiguous.
2. `library_algebra` = **no** — barcode multiplicity is a real in-tool "repeat every member" behaviour and PWM `dupMotifs` is a real library-wide dedup. Scored `no` on the orchestrator's combining criterion + the absence of any library object. `partial` is defensible under C3.
3. `exhaustive_single_scans` = **partial** — depends entirely on whether "single event" admits a motif substitution. Nucleotide-level reading = `no`. Also check this does not read as double-credit alongside `combinatorial_motif_place`.
4. `heterogeneous_components_one_library` = **partial** — `yes` is arguable, since singles + higher-order + WT + replicates genuinely coexist in one SNP or Motifs output; `partial` rests on the impossibility of including a sampled/control component.
5. `degenerate_iupac_codons` = **yes** — nucleotide-level IUPAC, and MPRAnator has no codon concept at all. If the row is enforced as strictly codon-level, this must move to `no` (and the row's own note naming MPRAnator as an exemplar should be corrected).
6. `ml_model_in_loop` = **no** — PWM-probability thresholding is model-driven generation in a weak sense; `partial` is arguable.
7. `sampled_random_mutagenesis` = **yes** — count-controlled, not rate-controlled, and confined to a separate module. If the row is read as requiring a *rate/spectrum* model this would be `partial`.
8. `documented_examples` = "6" — the count depends on whether the four in-form "Load Sample Data" presets count as documented examples and whether Supp. Fig. 3 does. A stricter count is 1 (the worked Motif Use Case). Must be counted the same way for all 12 tools.
9. `readout_analysis` = **no** — inferred from the complete enumeration of the tool's surface rather than from a targeted grep for read-analysis terms; the FINAL memo never addresses readout analysis explicitly because it never arose.
10. `barcode_generation` = **yes** and `combinatorial_motif_place` = **yes** — both depend on C5. Under a "must work today" convention both would fall, since both return HTTP 500 live.
11. Block E `interface` / `license` = **yes** — only because v2 makes Block E's value a placeholder. If the rendered matrix shows the value rather than the string, these must be restored to the v1 `partial` labels.
